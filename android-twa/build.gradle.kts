// Top-level build file. A TWA needs no Kotlin — the LauncherActivity ships in the
// androidbrowserhelper library — so only the Android application plugin is declared.
plugins {
    id("com.android.application") version "8.5.2" apply false
}
