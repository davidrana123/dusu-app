// Top-level build file. Kotlin is used for the connectivity-gate MainActivity +
// notification receivers; the TWA itself still comes from the androidbrowserhelper lib.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
